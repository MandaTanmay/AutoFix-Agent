'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Activity, ArrowRight, Bot, Check, ChevronRight, CircleCheck, Code2, GitBranch, Play, ShieldCheck, Sparkles, Terminal, Zap } from 'lucide-react'

const steps = [
  { icon: Code2, title: 'Connect your code', text: 'Paste a Python file or connect a repository. AutoFix understands the full context around your bug.' },
  { icon: Sparkles, title: 'Let AI investigate', text: 'The agent executes your code, reads the traceback, and traces the root cause—not just the symptom.' },
  { icon: CircleCheck, title: 'Ship with confidence', text: 'Review a precise diff, run validation, and keep the fix only when the tests pass.' },
]

const features = [
  ['Autonomous debugging', 'An agent that executes, observes, diagnoses, and patches in one focused loop.', Bot],
  ['Safe, reviewable fixes', 'Every change is shown as a small diff with confidence and validation details.', ShieldCheck],
  ['Built for your workflow', 'Start with a file, connect Git, and keep your debugging context in one workspace.', GitBranch],
]

function Logo() {
  return <a href="#top" className="flex items-center gap-2.5" aria-label="AutoFix home"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[var(--brand)] text-white"><Bot className="h-[18px] w-[18px]" /></span><span className="text-[15px] font-semibold tracking-tight text-[var(--ink)]">AutoFix</span></a>
}

function ProductPreview({ onNavigate }: { onNavigate: () => void }) {
  return <div className="preview-window" aria-label="AutoFix product preview"><div className="preview-toolbar"><div className="flex items-center gap-2"><span className="window-dot bg-[#ee776c]" /><span className="window-dot bg-[#edc04f]" /><span className="window-dot bg-[#63c554]" /></div><span className="preview-title">autofix / main.py</span><span className="preview-live"><span /> Live agent</span></div><div className="preview-grid"><div className="preview-code"><div className="flex items-center justify-between border-b border-[var(--line)] px-5 py-3"><span className="text-[11px] font-semibold text-[var(--ink)]">Python script</span><span className="rounded-full bg-[var(--blue-soft)] px-2 py-1 text-[10px] font-medium text-[var(--brand)]">main.py</span></div><div className="code-lines">{['def calculate_total(price, quantity):','    return priice * quantity','','price = 100','quantity = 5','','print(calculate_total(price, quantity))'].map((line, i) => <div key={i} className={i === 1 ? 'code-error' : ''}><span>{i + 1}</span><code>{line || ' '}</code>{i === 1 && <span className="error-mark">!</span>}</div>)}</div><div className="border-t border-[var(--line)] p-4"><button onClick={onNavigate} className="primary-button w-full"><Play className="h-3.5 w-3.5 fill-current" /> Run AutoFix Agent</button></div></div><div className="preview-agent"><div className="flex items-center justify-between border-b border-[var(--line)] px-5 py-3"><span className="text-[11px] font-semibold text-[var(--ink)]">Agent execution</span><span className="preview-status"><span /> Ready</span></div><div className="space-y-3 p-5">{['Execute', 'Observe', 'Diagnose', 'Patch', 'Validate'].map((item, i) => <div key={item} className="flex items-center gap-3"><span className={`preview-node ${i === 2 ? 'preview-node-active' : i < 2 ? 'preview-node-done' : ''}`}>{i < 2 ? <Check className="h-3 w-3" /> : i === 2 ? <Sparkles className="h-3 w-3" /> : <span>{i + 1}</span>}</span><div className="flex-1"><p className="text-[11px] font-semibold text-[var(--ink)]">{item}</p><p className="text-[10px] text-[var(--muted)]">{i < 2 ? 'Completed' : i === 2 ? 'Processing...' : 'Pending'}</p></div></div>)}</div></div></div></div>
}

export default function AutoFixLanding() {
  const [menuOpen, setMenuOpen] = useState(false)
  const router = useRouter()
  
  const navigateToDashboard = () => {
    router.push('/dashboard')
  }
  
  return <main id="top" className="landing-page"><header className="landing-nav"><div className="landing-container flex h-[72px] items-center justify-between"><Logo /><nav className={`${menuOpen ? 'flex' : 'hidden'} absolute left-4 right-4 top-[64px] flex-col gap-2 rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-3 shadow-lg md:static md:flex md:flex-row md:items-center md:gap-7 md:border-0 md:bg-transparent md:p-0 md:shadow-none`}><a href="#how-it-works" className="landing-link">How it works</a><a href="#features" className="landing-link">Features</a><a href="#preview" className="landing-link">Product</a></nav><div className="flex items-center gap-2"><button onClick={navigateToDashboard} className="login-button">Log in</button><button onClick={navigateToDashboard} className="signup-button">Sign up <ArrowRight className="h-3.5 w-3.5" /></button><button className="menu-button md:hidden" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle navigation"><span /><span /></button></div></div></header><section className="hero-section"><div className="landing-container"><div className="hero-copy"><div className="eyebrow"><span className="eyebrow-dot" /> AI-powered code repair</div><h1>Ship code. <span>Skip the debugging spiral.</span></h1><p className="hero-lede">AutoFix is an autonomous coding agent that finds the root cause, writes the repair, and proves it works—so your team can keep moving.</p><div className="hero-actions"><button onClick={navigateToDashboard} className="signup-button signup-button-large">Start fixing for free <ArrowRight className="h-4 w-4" /></button><a href="#preview" className="demo-link"><span className="demo-icon"><Play className="h-3 w-3 fill-current" /></span> See how it works</a></div><div className="hero-note"><Check className="h-3.5 w-3.5 text-[var(--success)]" /> No credit card required <span /> <Check className="h-3.5 w-3.5 text-[var(--success)]" /> Your code stays yours</div></div><div id="preview" className="hero-preview"><ProductPreview onNavigate={navigateToDashboard} /></div></div></section><section className="trust-strip"><div className="landing-container flex flex-wrap items-center justify-between gap-4 text-center md:text-left"><span>Trusted by developers at</span><b>Stripe</b><b>Vercel</b><b>Linear</b><b>Notion</b></div></section><section id="how-it-works" className="content-section"><div className="landing-container"><div className="section-intro"><p className="section-kicker">How it works</p><h2>Three steps to <span>bug-free code</span></h2><p>Upload your code, let AI investigate the issue, and ship with confidence knowing the fix has been validated.</p></div><div className="steps-grid">{steps.map((step, i) => <div key={i} className="step-card"><span className="step-number">0{i + 1}</span><div className="step-icon"><step.icon className="h-5 w-5" /></div><h3>{step.title}</h3><p>{step.text}</p><a href="#preview" className="step-link">Learn more <ChevronRight className="h-3 w-3" /></a></div>)}</div></div></section><section id="features" className="feature-section"><div className="landing-container"><div className="feature-layout"><div><p className="section-kicker">Features</p><h2>Built for <span>serious debugging</span></h2><p className="feature-lede">AutoFix isn't just a code fixer—it's an autonomous agent that understands context, learns from attempts, and validates every repair.</p><div className="feature-list">{features.map(([title, desc, Icon], i) => <div key={i} className="feature-row"><div className="feature-icon"><Icon className="h-4 w-4" /></div><div><h3>{title}</h3><p>{desc}</p></div></div>)}</div><a href="#preview" className="text-link">Try it yourself <ChevronRight className="h-3 w-3" /></a></div><div className="hidden lg:block"><div className="preview-window"><div className="preview-toolbar"><div className="flex items-center gap-2"><span className="window-dot bg-[#ee776c]" /><span className="window-dot bg-[#edc04f]" /><span className="window-dot bg-[#63c554]" /></div><span className="preview-title">autofix-agent / workspace</span></div><div className="p-5"><div className="space-y-3"><div className="flex items-center gap-3 text-sm"><Zap className="h-4 w-4 text-[var(--brand)]" /><span className="text-[var(--ink)]">Executing code in isolated environment...</span></div><div className="flex items-center gap-3 text-sm"><Activity className="h-4 w-4 text-[var(--success)]" /><span className="text-[var(--ink)]">Error detected: NameError at line 2</span></div><div className="flex items-center gap-3 text-sm"><Sparkles className="h-4 w-4 text-[var(--brand)]" /><span className="text-[var(--ink)]">AI diagnosis: Typo in variable name 'priice'</span></div><div className="flex items-center gap-3 text-sm"><Check className="h-4 w-4 text-[var(--success)]" /><span className="text-[var(--ink)]">Patch generated and validated successfully</span></div></div></div></div></div></div></div></section><section className="cta-section"><div className="landing-container"><div className="cta-card"><div><p className="section-kicker">Ready to fix your code?</p><h2>Start using AutoFix Agent <span>today</span></h2><p>Join thousands of developers who ship faster with autonomous code repair.</p></div><button onClick={navigateToDashboard} className="signup-button signup-button-large">Get started free <ArrowRight className="h-4 w-4" /></button></div></div></section><footer className="landing-footer"><div className="landing-container flex flex-col gap-8 py-8 md:flex-row md:items-center md:justify-between md:py-6"><p>© 2024 AutoFix Agent. All rights reserved.</p><div className="flex gap-6"><a href="#" className="landing-link">Privacy</a><a href="#" className="landing-link">Terms</a><a href="#" className="landing-link">Contact</a></div></div></footer></main>
}